{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "OS Packages metadata",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["name", "env", "packages"],
    "additionalProperties": false,
    "properties": {
      "name": {
        "type": "string",
        "description": "The friendly name of the package."
      },
      "why": {
        "type": "string",
        "description": "The reason or use of this package."
      },
      "url": {
        "type": "string",
        "format": "uri",
        "description": "The URL of the official website, repository, or documentation page."
      },
      "env": {
        "type": "string",
        "enum": ["all", "dev", "prod"],
        "description": "The environment in which the item should be installed."
      },
      "packages": {
        "type": "object",
        "minProperties": 1,
        "description": "The names of the packages specific to each Linux distribution.",
        "additionalProperties": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9+.-]*$"
        }
      }
    }
  }
}
